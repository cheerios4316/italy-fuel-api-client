import type { Descriptor } from "@/types";

type RegionList = Descriptor<string>[];

type ProvinceList = Descriptor<string>[];

type TownList = Descriptor<string>[];

type FuelsList = Descriptor<string>[];

type ServicesList = Descriptor<string>[];

export {
    type RegionList,
    type ProvinceList,
    type TownList,
    type FuelsList,
    type ServicesList,
}